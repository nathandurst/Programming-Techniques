#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main()
{
	int test, length, n, i, j;
	string line;
	cin >> test;
	cin.ignore(); cin.ignore();
	while (test--)
	{
		n = 0;
		getline(cin, line);
		const int length = line.size();
		vector<vector<char>> a;
		while (line[0] == 'L' || line[0] == 'W')
		{
			//int j = 0;
			for (i = 0; i < line.size(); i++)
			{
				a[n].push_back(line[i]);
				//j++;
			}
			getline(cin, line);
		}

		for (i = 0; i < 20; i++)
		{
			for (j = 0; j < 20; j++)
				cout << a[i][j] << ' ';
			cout << '\n';
		}

	}
	return 0;
}
